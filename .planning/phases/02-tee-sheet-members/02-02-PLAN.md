---
phase: 02-tee-sheet-members
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/member.ts
  - apps/dashboard/app/members/page.tsx
  - apps/dashboard/app/members/components/MemberList.tsx
  - apps/dashboard/app/members/components/MemberProfile.tsx
  - apps/dashboard/app/members/components/MemberActivityTimeline.tsx
  - apps/dashboard/app/members/components/MemberDirectory.tsx
  - apps/dashboard/app/members/components/ChurnRiskBadge.tsx
  - apps/dashboard/app/members/hooks/useMembers.ts
  - apps/dashboard/app/members/data/mock-members.ts
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04, MEM-05]

must_haves:
  truths:
    - "Staff can browse a searchable, filterable member directory"
    - "Staff can view a detailed member profile with photo, tier, contact info, family links"
    - "Staff can see an activity timeline on each member profile (visits, purchases, bookings)"
    - "Membership tiers (Executive, Platinum, Gold, Social, Junior, Family) are visible and filterable"
    - "Guest tracking shows which members brought which guests"
  artifacts:
    - path: "packages/shared/src/types/member.ts"
      provides: "MemberProfile, MembershipTier, FamilyLink, GuestVisit, ActivityEvent types"
      exports: ["MemberProfile", "MembershipTier", "FamilyLink", "GuestVisit", "ActivityEvent"]
    - path: "apps/dashboard/app/members/hooks/useMembers.ts"
      provides: "State management for member list, search, filtering, selection"
      exports: ["useMembers"]
    - path: "apps/dashboard/app/members/components/MemberProfile.tsx"
      provides: "Full member detail view with tabs (Overview, Activity, Spending, Guests)"
    - path: "apps/dashboard/app/members/components/MemberDirectory.tsx"
      provides: "Searchable, filterable member directory grid/list view"
  key_links:
    - from: "apps/dashboard/app/members/page.tsx"
      to: "hooks/useMembers.ts"
      via: "hook import for state management"
      pattern: "useMembers"
    - from: "apps/dashboard/app/members/components/MemberProfile.tsx"
      to: "packages/shared/src/types/member.ts"
      via: "type import for MemberProfile"
      pattern: "import.*MemberProfile"
---

<objective>
Build the member management module with profiles, directory, and activity tracking. Convert the existing prototype members page (static mock) into a fully functional member directory with detailed profiles showing activity timelines, family links, guest tracking, and membership tier information.

Purpose: Member management is the second daily-use module. Staff need to quickly look up members, see their full history, and manage guest visits.
Output: Interactive member directory with search/filter, detailed member profiles with tabs, activity timeline, family linking, and guest tracking.
</objective>

<execution_context>
@e:/Personal Assistant/projects/club-software/clubos-demo/.claude/get-shit-done/workflows/execute-plan.md
@e:/Personal Assistant/projects/club-software/clubos-demo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/Personal Assistant/projects/club-software/clubos-demo/.planning/PROJECT.md
@e:/Personal Assistant/projects/club-software/clubos-demo/.planning/ROADMAP.md
@e:/Personal Assistant/projects/club-software/clubos-demo/apps/dashboard/app/members/page.tsx
@e:/Personal Assistant/projects/club-software/clubos-demo/packages/shared/src/types/odoo.ts
@e:/Personal Assistant/projects/club-software/clubos-demo/apps/dashboard/app/layout.tsx

<interfaces>
From packages/shared/src/types/odoo.ts:
```typescript
export interface Member {
  id: number;
  name: string;
  email: string;
  phone: string;
  membershipType: string;
  memberNumber: string;
  photo?: string;
  joinDate: string;
  status: "active" | "inactive" | "suspended";
  churnRisk: 1 | 2 | 3 | 4 | 5;
}
```

Design system: emerald (#10B981) primary, white backgrounds, dark sidebar (#0A0A0A), Inter font.
Layout: Sidebar (240px left) + TopBar already in place.
Material Symbols Outlined font loaded globally.
Existing prototype has: member list (left panel), member detail (center), AI recommendations (right panel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define member types and create comprehensive mock data</name>
  <files>packages/shared/src/types/member.ts, apps/dashboard/app/members/data/mock-members.ts, apps/dashboard/app/members/hooks/useMembers.ts</files>
  <action>
1. Create `packages/shared/src/types/member.ts` with expanded types:
   - `MembershipTier { id: string; name: "Executive" | "Platinum" | "Gold" | "Social" | "Junior" | "Family" | "Corporate" | "Honorary"; privileges: string[]; monthlyDues: number; advanceBookingDays: number; guestPassesPerMonth: number; color: string }`
   - `FamilyLink { memberId: number; name: string; relationship: "spouse" | "child" | "parent" | "sibling"; memberNumber?: string }`
   - `GuestVisit { id: string; guestName: string; date: string; sponsoringMemberId: number; guestFee: number; activity: string }`
   - `ActivityEvent { id: string; type: "check-in" | "dining" | "tee-time" | "event" | "pro-shop" | "fitness" | "spa" | "email" | "payment"; description: string; timestamp: string; amount?: number; icon: string }`
   - `MemberProfile extends Member { initials: string; address?: string; emergencyContact?: { name: string; phone: string }; familyLinks: FamilyLink[]; guestVisits: GuestVisit[]; activityHistory: ActivityEvent[]; monthlyVisits: number[]; lifetimeSpend: number; annualSpend: number; visitFrequency: string; npsScore: number; notes?: string }`
   - Export all from packages/shared/src/index.ts

2. Create `apps/dashboard/app/members/data/mock-members.ts`:
   - 15+ mock MemberProfile records with realistic data
   - Include family links between some members (spouse/child relationships)
   - Include guest visit records (at least 10 across members)
   - Each member has 5-8 activity events and 12 months of visit data
   - Include members at each risk level (low/medium/high)
   - Export `MEMBERSHIP_TIERS` array with all tier configs
   - Export `MOCK_MEMBER_PROFILES` array

3. Create `apps/dashboard/app/members/hooks/useMembers.ts`:
   - State: `members: MemberProfile[]`, `selectedMemberId: number | null`, `searchQuery: string`, `filterTier: string | "all"`, `filterRisk: "all" | "low" | "medium" | "high"`, `filterStatus: "all" | "active" | "inactive" | "suspended"`, `viewMode: "list" | "grid"`, `sortBy: "name" | "joinDate" | "risk" | "spend"`
   - Derived: `filteredMembers` computed from all filters + search
   - Actions: `selectMember(id)`, `setSearch(q)`, `setFilter*(...)`, `updateMember(id, updates)`, `addGuestVisit(memberId, guest)`, `setSort(field)`
   - Initialize with mock data
  </action>
  <verify>
    <automated>cd "e:/Personal Assistant/projects/club-software/clubos-demo" && npx tsc --noEmit --project apps/dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Type file exports all member types. 15+ mock members with family links, guest visits, and activity histories. useMembers hook provides search, filter, sort, and CRUD operations.</done>
</task>

<task type="auto">
  <name>Task 2: Build member directory and profile components</name>
  <files>apps/dashboard/app/members/page.tsx, apps/dashboard/app/members/components/MemberList.tsx, apps/dashboard/app/members/components/MemberProfile.tsx, apps/dashboard/app/members/components/MemberActivityTimeline.tsx, apps/dashboard/app/members/components/MemberDirectory.tsx, apps/dashboard/app/members/components/ChurnRiskBadge.tsx</files>
  <action>
Replace existing static `members/page.tsx` with fully interactive version:

1. **ChurnRiskBadge.tsx**: Reusable risk indicator.
   - Props: `risk: 1 | 2 | 3 | 4 | 5`
   - 1-2: emerald (Low), 3: yellow (Medium), 4-5: red (High)
   - Shows colored dot + label text

2. **MemberList.tsx**: Left sidebar member list (320px).
   - Search input at top (filters by name, email, member number)
   - Filter pills: All | VIP (Executive/Platinum) | At Risk (4-5) | New (joined last 12 months)
   - Tier filter dropdown
   - Sort dropdown (Name, Join Date, Risk, Spend)
   - Scrollable list of member cards showing: initials avatar, name, tier badge, churn risk dot
   - Selected member has left emerald border + shadow
   - Member count shown at bottom

3. **MemberProfile.tsx**: Center panel with tabbed detail view.
   - Header: large initials avatar (or photo placeholder), full name, tier badge (color-coded), member since date, NPS score
   - Tabs: Overview | Activity | Spending | Guests | Communications
   - **Overview tab**: 4 stat cards (Lifetime Spend, Annual Spend, Visit Frequency, Churn Risk) + visit trend bar chart (12 months) + family links section showing linked members with relationship labels
   - **Activity tab**: renders MemberActivityTimeline
   - **Spending tab**: spending breakdown by category (bar chart), lifetime + annual totals
   - **Guests tab**: table of guest visits (date, guest name, activity, fee) + guest count vs limit indicator
   - **Communications tab**: list of emails/SMS sent to member with status (opened, clicked, delivered)

4. **MemberActivityTimeline.tsx**: Vertical timeline of ActivityEvents.
   - Each event: icon (Material Symbols), description, timestamp, optional amount
   - Events grouped by date
   - Color-coded by type (tee-time=emerald, dining=amber, payment=blue, etc.)

5. **MemberDirectory.tsx**: Alternative grid/table view of all members.
   - Table columns: Name, Tier, Status, Risk, Visit Freq, Annual Spend, Last Visit
   - Sortable columns
   - Switchable between list view (current left panel) and full-width directory table
   - Used when viewMode is "grid" -- replaces the 3-panel layout with a full-width table

6. **page.tsx**: Orchestrator.
   - Uses useMembers hook
   - Top controls: "Members" heading, Add Member button, view mode toggle (list/grid)
   - List view (default): 3-panel layout -- MemberList | MemberProfile | AI Recommendations panel
   - Grid view: Full-width MemberDirectory table
   - AI Recommendations panel (right, 320px): same concept as prototype -- shows churn risk summary, personalized recommendations based on selected member

Keep the AI recommendations panel from the existing prototype. Match the emerald design system.

IMPORTANT: Read `node_modules/next/dist/docs/` for any Next.js breaking changes. Mark all interactive components with "use client".
  </action>
  <verify>
    <automated>cd "e:/Personal Assistant/projects/club-software/clubos-demo" && npx tsc --noEmit --project apps/dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Members page renders searchable directory. Clicking a member shows full profile with tabs. Activity timeline shows chronological events. Guest tracking tab shows visit history with fees. Family links visible on overview. Grid view shows full-width sortable table.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for the dashboard app
2. Navigate to `/members` -- directory renders with 15+ members
3. Search by name -- list filters in real-time
4. Filter by tier (VIP, At Risk) -- list updates correctly
5. Click member -- profile shows with all tabs working
6. Activity tab shows chronological timeline with icons
7. Guests tab shows guest visit records with fees
8. Switch to grid view -- full-width table renders with sortable columns
</verification>

<success_criteria>
- Member directory with search, filter by tier/risk/status, and sort capabilities
- Detailed member profiles with Overview, Activity, Spending, Guests, Communications tabs
- Activity timeline with date-grouped, color-coded events
- Family links displayed on member profiles
- Guest tracking with visit history and fee totals
- Grid/list view toggle for the directory
</success_criteria>

<output>
After completion, create `.planning/phases/02-tee-sheet-members/02-02-SUMMARY.md`
</output>

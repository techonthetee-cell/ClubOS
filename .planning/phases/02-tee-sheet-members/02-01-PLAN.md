---
phase: 02-tee-sheet-members
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/tee-sheet.ts
  - apps/dashboard/app/tee-sheet/page.tsx
  - apps/dashboard/app/tee-sheet/components/TeeSheetGrid.tsx
  - apps/dashboard/app/tee-sheet/components/TimeColumn.tsx
  - apps/dashboard/app/tee-sheet/components/SlotCell.tsx
  - apps/dashboard/app/tee-sheet/components/BookingSidebar.tsx
  - apps/dashboard/app/tee-sheet/components/BookingModal.tsx
  - apps/dashboard/app/tee-sheet/hooks/useTeeSheet.ts
  - apps/dashboard/app/tee-sheet/data/mock-tee-sheet.ts
autonomous: true
requirements: [TEE-01, TEE-02, TEE-03]

must_haves:
  truths:
    - "Staff can see a visual tee sheet grid with time slots on Y axis and holes on X axis"
    - "Staff can click an empty slot and create a booking with player details"
    - "Staff can click a booked slot to view details, edit, or cancel the booking"
    - "Time intervals are configurable (7, 8, 9, 10 min) via a dropdown"
    - "Bookings support member lookup and guest entry with group size 1-4"
  artifacts:
    - path: "packages/shared/src/types/tee-sheet.ts"
      provides: "TeeTimeSlot, Booking, BookingPlayer, TeeSheetConfig types"
      exports: ["TeeTimeSlot", "Booking", "BookingPlayer", "TeeSheetConfig", "SlotStatus"]
    - path: "apps/dashboard/app/tee-sheet/hooks/useTeeSheet.ts"
      provides: "State management for tee sheet bookings (create, edit, cancel)"
      exports: ["useTeeSheet"]
    - path: "apps/dashboard/app/tee-sheet/components/TeeSheetGrid.tsx"
      provides: "Main grid component rendering time x holes matrix"
    - path: "apps/dashboard/app/tee-sheet/components/BookingModal.tsx"
      provides: "Modal for creating/editing bookings with player search"
  key_links:
    - from: "apps/dashboard/app/tee-sheet/page.tsx"
      to: "hooks/useTeeSheet.ts"
      via: "hook import for state management"
      pattern: "useTeeSheet"
    - from: "apps/dashboard/app/tee-sheet/components/BookingModal.tsx"
      to: "hooks/useTeeSheet.ts"
      via: "createBooking/updateBooking/cancelBooking actions"
      pattern: "createBooking|updateBooking|cancelBooking"
---

<objective>
Build the functional tee sheet grid UI with booking CRUD operations. Convert the existing prototype tee-sheet page (static mock data) into a fully interactive tee sheet where staff can view, create, edit, and cancel bookings on a time-slot-by-hole grid.

Purpose: The tee sheet is the #1 daily-use screen for club staff. This is the demo centerpiece.
Output: Interactive tee sheet grid with booking creation/editing modal, configurable time intervals, and member/guest player entry.
</objective>

<execution_context>
@e:/Personal Assistant/projects/club-software/clubos-demo/.claude/get-shit-done/workflows/execute-plan.md
@e:/Personal Assistant/projects/club-software/clubos-demo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/Personal Assistant/projects/club-software/clubos-demo/.planning/PROJECT.md
@e:/Personal Assistant/projects/club-software/clubos-demo/.planning/ROADMAP.md
@e:/Personal Assistant/projects/club-software/clubos-demo/.planning/STATE.md
@e:/Personal Assistant/projects/club-software/clubos-demo/apps/dashboard/app/tee-sheet/page.tsx
@e:/Personal Assistant/projects/club-software/clubos-demo/packages/shared/src/types/odoo.ts
@e:/Personal Assistant/projects/club-software/clubos-demo/packages/ai/src/index.ts
@e:/Personal Assistant/projects/club-software/clubos-demo/apps/dashboard/app/layout.tsx

<interfaces>
<!-- Existing types from packages/shared/src/types/odoo.ts -->
From packages/shared/src/types/odoo.ts:
```typescript
export interface TeeTime {
  id: number;
  date: string;
  time: string;
  holes: number;
  players: number;
  maxPlayers: number;
  status: "available" | "booked" | "blocked" | "maintenance";
  memberId?: number;
  memberName?: string;
  greenFee: number;
  cartFee: number;
}
```

From packages/ai/src/index.ts:
```typescript
export interface DynamicPrice {
  basePrice: number;
  adjustedPrice: number;
  multiplier: number;
  reason: string;
}
export function getDynamicPrice(basePrice: number): DynamicPrice;
```

Design system: emerald (#10B981) primary, white backgrounds, dark sidebar (#0A0A0A), Inter font.
Layout: Sidebar (240px left) + TopBar already in place. Page renders inside `<main className="flex-1 p-4 md:p-6">`.
Material Symbols Outlined font loaded globally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define tee sheet types and create mock data store</name>
  <files>packages/shared/src/types/tee-sheet.ts, apps/dashboard/app/tee-sheet/data/mock-tee-sheet.ts, apps/dashboard/app/tee-sheet/hooks/useTeeSheet.ts</files>
  <action>
1. Create `packages/shared/src/types/tee-sheet.ts` with expanded types:
   - `SlotStatus = "available" | "booked" | "blocked" | "maintenance" | "tournament"`
   - `TeeSheetConfig { intervalMinutes: 7 | 8 | 9 | 10; startTime: string; endTime: string; holesCount: 9 | 18; courseId: number; courseName: string }`
   - `BookingPlayer { id?: number; name: string; memberNumber?: string; membershipType?: string; isGuest: boolean; email?: string; phone?: string }`
   - `Booking { id: string; slotId: string; date: string; time: string; holeNumber: number; players: BookingPlayer[]; groupSize: number; status: "confirmed" | "checked-in" | "cancelled" | "no-show"; greenFee: number; cartFee: number; totalFee: number; notes?: string; createdAt: string; cart: boolean }`
   - `TeeTimeSlot { id: string; date: string; time: string; holeNumber: number; status: SlotStatus; booking?: Booking; blockReason?: string }`
   - Export all from packages/shared/src/index.ts

2. Create `apps/dashboard/app/tee-sheet/data/mock-tee-sheet.ts`:
   - `generateTimeSlots(config: TeeSheetConfig, date: string): TeeTimeSlot[]` -- generates full day of slots
   - Pre-populate ~15 booked slots with realistic player names across the day
   - Include 3 blocked slots (maintenance) and 1 tournament block
   - Default config: 10-min intervals, 06:00-18:00, 18 holes (show first 10 in grid for readability)
   - Export a `MOCK_MEMBERS` array with 20 members for the player search lookup

3. Create `apps/dashboard/app/tee-sheet/hooks/useTeeSheet.ts`:
   - Zustand store (or useState if Zustand not installed) managing:
     - `slots: TeeTimeSlot[]`
     - `config: TeeSheetConfig`
     - `selectedSlot: TeeTimeSlot | null`
     - `selectedDate: string` (YYYY-MM-DD)
   - Actions: `createBooking(slotId, players, options)`, `updateBooking(bookingId, updates)`, `cancelBooking(bookingId)`, `checkIn(bookingId)`, `setConfig(partial)`, `setDate(date)`, `selectSlot(slot | null)`
   - `createBooking` should update the slot status to "booked" and attach the booking
   - `cancelBooking` should set booking status to "cancelled" and slot back to "available"
   - Initialize with mock data from the data module

Use React useState for state management (simpler, no extra deps needed for MVP). Export a custom hook `useTeeSheet()`.
  </action>
  <verify>
    <automated>cd "e:/Personal Assistant/projects/club-software/clubos-demo" && npx tsc --noEmit --project apps/dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Type file exports all tee sheet types. Mock data generates a full day of slots. useTeeSheet hook provides CRUD operations for bookings with proper state updates.</done>
</task>

<task type="auto">
  <name>Task 2: Build interactive tee sheet grid with booking modal</name>
  <files>apps/dashboard/app/tee-sheet/page.tsx, apps/dashboard/app/tee-sheet/components/TeeSheetGrid.tsx, apps/dashboard/app/tee-sheet/components/TimeColumn.tsx, apps/dashboard/app/tee-sheet/components/SlotCell.tsx, apps/dashboard/app/tee-sheet/components/BookingSidebar.tsx, apps/dashboard/app/tee-sheet/components/BookingModal.tsx</files>
  <action>
Replace the existing static `tee-sheet/page.tsx` with a fully interactive version composed of extracted components:

1. **TeeSheetGrid.tsx**: Main grid layout.
   - Renders TimeColumn on left (sticky), hole headers across top (sticky)
   - Maps over slots to render SlotCell for each time x hole position
   - Receives slots, selectedSlot, onSlotClick from props

2. **TimeColumn.tsx**: Left-side time labels.
   - Renders time labels based on config.intervalMinutes
   - Highlights current time row with a subtle indicator

3. **SlotCell.tsx**: Individual grid cell.
   - Available: white bg, shows "Book Slot" on hover (emerald text)
   - Booked: emerald-500 bg, shows player name + cart icon + caddie
   - Blocked: red-50 bg with red border, shows reason
   - Tournament: amber-50 bg with amber border, shows label
   - Selected: ring-2 ring-emerald-500 highlight
   - onClick fires onSlotClick(slot)

4. **BookingSidebar.tsx**: Right sidebar (320px) showing selected slot details.
   - When no slot selected: show day summary (total bookings, utilization %, revenue estimate)
   - When available slot selected: show "New Booking" form trigger
   - When booked slot selected: show booking details (players, fees, status) with Edit/Check-In/Cancel buttons
   - Keep the AI Yield Strategy card from the prototype design

5. **BookingModal.tsx**: Dialog/modal for creating or editing a booking.
   - Player entry: search input that filters MOCK_MEMBERS, add guest option
   - Group size selector (1-4)
   - Cart toggle (yes/no)
   - Notes textarea
   - Fee summary (auto-calculated: greenFee + cartFee x players)
   - "Confirm Booking" / "Save Changes" button
   - "Cancel" button to close
   - Use a simple modal overlay (fixed position, backdrop blur) -- no dependency needed

6. **page.tsx**: Orchestrator.
   - Uses useTeeSheet hook
   - Top controls bar: Day/Week toggle, date picker input, interval dropdown (7/8/9/10 min), dynamic pricing toggle
   - Renders TeeSheetGrid + BookingSidebar side by side
   - Opens BookingModal on new booking or edit actions
   - All state flows through useTeeSheet hook

Design: Match the existing prototype's emerald color scheme exactly. Use Tailwind only (no component library). Material Symbols Outlined for icons (already loaded globally).

IMPORTANT: Read `node_modules/next/dist/docs/` for any Next.js breaking changes before writing components. Mark all interactive components with "use client" directive.
  </action>
  <verify>
    <automated>cd "e:/Personal Assistant/projects/club-software/clubos-demo" && npx tsc --noEmit --project apps/dashboard/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Tee sheet page renders an interactive grid. Clicking an empty slot opens the booking modal. Clicking a booked slot shows details in the sidebar. Creating a booking updates the grid immediately. Cancelling a booking returns the slot to available. Interval dropdown changes the time spacing. All using mock data with no backend dependency.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for the dashboard app
2. Navigate to `/tee-sheet` in browser -- grid renders with time slots and holes
3. Click empty slot -- booking modal opens with player search
4. Create a booking -- slot turns emerald with player name
5. Click booked slot -- sidebar shows details with Edit/Check-In/Cancel
6. Cancel booking -- slot returns to available
7. Change interval dropdown -- grid re-renders with different time spacing
</verification>

<success_criteria>
- Staff can view a full-day tee sheet grid with configurable time intervals
- Staff can create bookings by clicking empty slots and filling in player details
- Staff can view, edit, and cancel existing bookings
- Member lookup (from mock data) works in the booking modal
- All interactions use local state (no backend required)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tee-sheet-members/02-01-SUMMARY.md`
</output>

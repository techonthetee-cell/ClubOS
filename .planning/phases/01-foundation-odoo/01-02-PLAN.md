---
phase: 01-foundation-odoo
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .npmrc
  - apps/dashboard/package.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/tsconfig.json
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/app/page.tsx
  - apps/dashboard/app/(auth)/login/page.tsx
  - apps/dashboard/app/(dashboard)/layout.tsx
  - apps/dashboard/app/(dashboard)/page.tsx
  - apps/dashboard/app/api/auth/[...nextauth]/route.ts
  - apps/dashboard/middleware.ts
  - apps/dashboard/lib/auth.ts
  - apps/booking/package.json
  - apps/booking/app/layout.tsx
  - apps/booking/app/page.tsx
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types/auth.ts
  - packages/shared/src/types/tenant.ts
  - packages/shared/src/types/odoo.ts
  - packages/shared/src/tenants.ts
  - packages/shared/src/index.ts
  - packages/shared/vitest.config.ts
  - packages/shared/tests/tenants.test.ts
  - packages/tsconfig/base.json
  - packages/tsconfig/nextjs.json
  - packages/tsconfig/package.json
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "pnpm install succeeds and turbo build completes without errors"
    - "Dashboard app starts on localhost:3000 and shows login page"
    - "Staff can log in with Odoo credentials and see the dashboard shell"
    - "Subdomain nevelmeade.localhost resolves to Nevel Meade tenant context"
    - "Creating a second club database in Odoo and adding it to tenant registry routes correctly"
    - "Booking app starts on a separate port and shows placeholder page"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo build orchestration"
      contains: "dependsOn"
    - path: "apps/dashboard/middleware.ts"
      provides: "Subdomain extraction and tenant resolution"
      contains: "getTenantBySubdomain"
    - path: "apps/dashboard/lib/auth.ts"
      provides: "Auth.js config with Odoo credentials provider"
      contains: "Credentials"
    - path: "packages/shared/src/tenants.ts"
      provides: "Tenant registry with subdomain-to-database mapping"
      exports: ["getTenantBySubdomain", "getAllTenants", "Tenant"]
    - path: "packages/shared/src/types/auth.ts"
      provides: "Auth session types"
      exports: ["User", "Session"]
  key_links:
    - from: "apps/dashboard/middleware.ts"
      to: "packages/shared/src/tenants.ts"
      via: "import getTenantBySubdomain"
      pattern: "getTenantBySubdomain"
    - from: "apps/dashboard/lib/auth.ts"
      to: "packages/odoo-client/src/client.ts"
      via: "ClubOSClient.authenticate in credentials provider"
      pattern: "ClubOSClient.*authenticate"
    - from: "apps/dashboard/app/(dashboard)/layout.tsx"
      to: "middleware.ts tenant headers"
      via: "x-tenant-id header from middleware"
      pattern: "x-tenant-id|x-tenant-db"
---

<objective>
Restructure the existing create-next-app into a Turborepo monorepo with two apps and shared packages, then wire up Auth.js with Odoo credentials and multi-tenant subdomain routing.

Purpose: This transforms the proof-of-concept Odoo client into a real application structure. Staff can log in, the system knows which club they belong to, and the monorepo supports independent deploys.
Output: Working monorepo with dashboard (staff login + empty tee sheet page) and booking (placeholder), multi-tenant routing proven with 2 databases.
</objective>

<execution_context>
@C:/Users/BankAnantravanich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BankAnantravanich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-odoo/01-RESEARCH.md
@.planning/phases/01-foundation-odoo/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From packages/odoo-client/src/client.ts:
```typescript
export interface OdooConfig {
  url: string;
  db: string;
}

export class ClubOSClient {
  constructor(config: OdooConfig);
  authenticate(login: string, password: string): Promise<number>;
  searchRead<T>(model: string, domain: OdooDomain, fields: string[], options?: OdooSearchReadOptions): Promise<T[]>;
  create(model: string, values: Record<string, any>): Promise<number>;
  write(model: string, ids: number[], values: Record<string, any>): Promise<boolean>;
  unlink(model: string, ids: number[]): Promise<boolean>;
}
```

From packages/odoo-client/src/errors.ts:
```typescript
export class OdooAuthError extends Error {}
export class OdooRPCError extends Error {}
export class OdooConnectionError extends Error {}
```

From packages/odoo-client/src/auth.ts:
```typescript
export function createServiceClient(config: OdooConfig, apiKeyOrPassword: string): Promise<ClubOSClient>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure into Turborepo monorepo with shared packages</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    .npmrc,
    apps/dashboard/package.json,
    apps/dashboard/next.config.ts,
    apps/dashboard/tsconfig.json,
    apps/booking/package.json,
    apps/booking/app/layout.tsx,
    apps/booking/app/page.tsx,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/types/auth.ts,
    packages/shared/src/types/tenant.ts,
    packages/shared/src/types/odoo.ts,
    packages/shared/src/tenants.ts,
    packages/shared/src/index.ts,
    packages/tsconfig/base.json,
    packages/tsconfig/nextjs.json,
    packages/tsconfig/package.json
  </files>
  <action>
CRITICAL: This restructures the existing create-next-app into a monorepo. The current app/ directory becomes apps/dashboard/app/.

1. **Install pnpm globally and delete npm artifacts:**
   - `npm install -g pnpm turbo`
   - Delete `node_modules/` and `package-lock.json` from project root

2. **Create root monorepo config:**
   - `pnpm-workspace.yaml`: packages: ['apps/*', 'packages/*']
   - `.npmrc`: `shamefully-hoist=true` (safety net per Pitfall 3 in research)
   - Root `package.json`: name "clubos", private true, devDependencies: turbo. Scripts: dev (`turbo dev`), build (`turbo build`), lint (`turbo lint`), test (`turbo test`)
   - `turbo.json`: Per research Pattern — build depends on ^build, dev is persistent/no-cache, lint depends on ^build, test depends on ^build. Outputs: [".next/**", "!.next/cache/**", "dist/**"]

3. **Create packages/tsconfig/ (shared TypeScript configs):**
   - `packages/tsconfig/package.json`: name "@clubos/tsconfig"
   - `packages/tsconfig/base.json`: strict, ES2022, Node16 module resolution, declaration
   - `packages/tsconfig/nextjs.json`: extends base, adds jsx preserve, Next.js paths

4. **Move existing app to apps/dashboard/:**
   - Move `app/`, `public/`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs` into `apps/dashboard/`
   - Create `apps/dashboard/package.json`: name "@clubos/dashboard", dependencies: next 16.x, react 19.x, next-auth@beta, @clubos/shared, @clubos/odoo-client, @clubos/ui (placeholder)
   - Update `apps/dashboard/tsconfig.json` to extend @clubos/tsconfig/nextjs.json
   - Update `apps/dashboard/next.config.ts` to add `transpilePackages: ['@clubos/ui', '@clubos/shared', '@clubos/odoo-client']` (per Pitfall 4 in research)

5. **Create apps/booking/ (stub):**
   - `apps/booking/package.json`: name "@clubos/booking"
   - `apps/booking/app/layout.tsx`: minimal layout
   - `apps/booking/app/page.tsx`: placeholder "ClubOS Booking Portal — Coming Soon"
   - Minimal next.config.ts, tsconfig.json

6. **Create packages/shared/:**
   - `packages/shared/package.json`: name "@clubos/shared", main "dist/index.js", types "dist/index.d.ts"
   - `packages/shared/tsconfig.json`: extends @clubos/tsconfig/base.json
   - `packages/shared/src/types/auth.ts`: User type (id, name, email, tenantId, role: 'staff' | 'member'), Session type
   - `packages/shared/src/types/tenant.ts`: Tenant type (id, db, name, subdomain)
   - `packages/shared/src/types/odoo.ts`: Re-export types from odoo-client that apps need (OdooConfig, OdooDomain)
   - `packages/shared/src/tenants.ts`: Static tenant registry per research (nevelmeade -> clubos_nevelmeade, demo -> clubos_demo). Exports: getTenantBySubdomain, getAllTenants, TENANTS map
   - `packages/shared/src/index.ts`: Re-export all types and tenants

7. **Integrate packages/odoo-client from Plan 01:**
   - Update `packages/odoo-client/package.json` to reference @clubos/tsconfig
   - Ensure it's listed in pnpm-workspace.yaml (already covered by packages/*)

8. **Run `pnpm install` and `pnpm turbo build` to verify everything compiles.**

PITFALL WARNING: After migration from npm to pnpm, if modules aren't found, check that `shamefully-hoist=true` is in .npmrc. Delete all node_modules before pnpm install.
  </action>
  <verify>
    <automated>pnpm install && pnpm turbo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Monorepo structure: apps/dashboard, apps/booking, packages/shared, packages/odoo-client, packages/tsconfig
    - `pnpm turbo build` completes without errors
    - Dashboard app runs on localhost:3000
    - Booking app runs on a separate port
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Auth.js with Odoo credentials + multi-tenant subdomain middleware</name>
  <files>
    apps/dashboard/lib/auth.ts,
    apps/dashboard/app/api/auth/[...nextauth]/route.ts,
    apps/dashboard/app/(auth)/login/page.tsx,
    apps/dashboard/app/(dashboard)/layout.tsx,
    apps/dashboard/app/(dashboard)/page.tsx,
    apps/dashboard/app/layout.tsx,
    apps/dashboard/middleware.ts,
    packages/shared/vitest.config.ts,
    packages/shared/tests/tenants.test.ts
  </files>
  <action>
1. **Create Auth.js configuration (apps/dashboard/lib/auth.ts):**
   - Install: `pnpm add next-auth@beta --filter @clubos/dashboard`
   - Configure Auth.js v5 with Credentials provider
   - The authorize function:
     a. Reads `x-tenant-db` header from the request (set by middleware)
     b. Creates ClubOSClient with ODOO_URL env var + tenant db
     c. Calls client.authenticate(email, password)
     d. On success: fetches user details via searchRead on res.users (fields: name, email, groups_id)
     e. Returns user object: { id: uid, name, email, tenantId, tenantDb }
     f. On failure: returns null (Auth.js shows error)
   - Session strategy: JWT
   - JWT callback: includes tenantId and tenantDb in the token
   - Session callback: exposes tenantId and tenantDb in session.user
   - Export: handlers, auth, signIn, signOut

   IMPORTANT: If Auth.js v5 has compatibility issues with Next.js 16 (per Open Question 3 in research), fall back to a simpler JWT approach:
   - Create /api/auth/login route that authenticates with Odoo, creates a JWT, sets httpOnly cookie
   - Create middleware that validates the JWT
   - Document the fallback in the SUMMARY

2. **Create Auth.js route handler:**
   - `apps/dashboard/app/api/auth/[...nextauth]/route.ts`: export GET and POST from handlers

3. **Create login page (apps/dashboard/app/(auth)/login/page.tsx):**
   - Simple form: email + password + submit button
   - Uses signIn("credentials", { email, password, redirectTo: "/" })
   - Error display for invalid credentials
   - Styled with Tailwind (emerald accent: bg-emerald-600 for submit button)
   - ClubOS logo/name at top

4. **Create dashboard shell (apps/dashboard/app/(dashboard)/layout.tsx):**
   - Requires authenticated session (redirect to /login if not)
   - Sidebar with nav items: Tee Sheet (active), Members, POS, Settings (all placeholder links)
   - Header with club name (from tenant), user name, sign out button
   - Main content area renders children
   - Read tenant info from session or headers

5. **Create tee sheet placeholder (apps/dashboard/app/(dashboard)/page.tsx):**
   - "Tee Sheet" heading
   - Empty state: "Tee sheet coming in Phase 2" with calendar icon
   - Shows tenant/club name to prove multi-tenancy is working

6. **Create subdomain middleware (apps/dashboard/middleware.ts):**
   - Extract hostname from request.headers.get('host')
   - Parse subdomain: hostname.split('.')[0]
   - For localhost dev: support `nevelmeade.localhost:3000` pattern
   - Call getTenantBySubdomain(subdomain) from @clubos/shared
   - If no tenant found: redirect to /not-found
   - If tenant found: set x-tenant-id and x-tenant-db headers via NextResponse.next()
   - Combine with Auth.js middleware: protect (dashboard)/* routes, allow (auth)/* routes
   - Matcher: exclude api/auth, _next, static files

7. **Create tenant resolution tests (packages/shared/tests/tenants.test.ts):**
   - getTenantBySubdomain('nevelmeade') returns Nevel Meade tenant with db clubos_nevelmeade
   - getTenantBySubdomain('demo') returns demo tenant
   - getTenantBySubdomain('nonexistent') returns undefined
   - getAllTenants() returns array of all tenants

8. **Create second Odoo database to prove multi-tenancy:**
   - Via Odoo web UI or CLI: create `clubos_nevelmeade` database
   - Add to tenant registry in packages/shared/src/tenants.ts (already there from Task 1)
   - Test: access nevelmeade.localhost:3000 -> resolves to clubos_nevelmeade DB
   - Test: access demo.localhost:3000 -> resolves to clubos_demo DB

9. **Environment variables (apps/dashboard/.env.local):**
   - ODOO_URL=http://localhost:8069
   - AUTH_SECRET=generate-a-random-string-here (for Auth.js JWT signing)
   - NEXTAUTH_URL=http://localhost:3000
  </action>
  <verify>
    <automated>cd packages/shared && pnpm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Staff can navigate to localhost:3000, see login page
    - Staff enters Odoo admin credentials, gets redirected to dashboard shell
    - Dashboard shows club name from tenant context and user name from Odoo
    - Sidebar with Tee Sheet, Members, POS, Settings links (placeholders)
    - Tee sheet page shows empty state with club name
    - nevelmeade.localhost:3000 resolves to Nevel Meade tenant
    - demo.localhost:3000 resolves to Demo tenant
    - Invalid subdomain redirects to /not-found
    - Tenant resolution unit tests pass
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` — full monorepo build succeeds
2. `pnpm turbo test` — all tests pass (shared tenant tests + odoo-client tests from Plan 01)
3. Start dashboard: `pnpm dev --filter @clubos/dashboard` — loads on localhost:3000
4. Navigate to localhost:3000 → redirected to /login
5. Log in with Odoo admin credentials → see dashboard shell with tee sheet placeholder
6. Access nevelmeade.localhost:3000 → tenant context shows "Nevel Meade Golf Course"
7. Access demo.localhost:3000 → tenant context shows "ClubOS Demo"
</verification>

<success_criteria>
- Turborepo monorepo builds cleanly with pnpm (apps/dashboard, apps/booking, packages/shared, packages/odoo-client, packages/tsconfig)
- Staff login works: Odoo credentials → Auth.js session → JWT → protected dashboard
- Multi-tenancy proven: 2 subdomains → 2 different Odoo databases → 2 different club contexts
- Dashboard shell rendered with sidebar navigation and tenant-aware header
- Booking app runs as separate Next.js app (placeholder only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-odoo/01-02-SUMMARY.md`
</output>

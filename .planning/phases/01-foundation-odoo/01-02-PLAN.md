---
phase: 01-foundation-odoo
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .npmrc
  - apps/dashboard/package.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/tsconfig.json
  - apps/booking/package.json
  - apps/booking/app/layout.tsx
  - apps/booking/app/page.tsx
  - apps/booking/next.config.ts
  - apps/booking/tsconfig.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types/auth.ts
  - packages/shared/src/types/tenant.ts
  - packages/shared/src/types/odoo.ts
  - packages/shared/src/tenants.ts
  - packages/shared/src/index.ts
  - packages/ai/package.json
  - packages/ai/tsconfig.json
  - packages/ai/src/index.ts
  - packages/tsconfig/base.json
  - packages/tsconfig/nextjs.json
  - packages/tsconfig/package.json
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "pnpm install succeeds and turbo build completes without errors"
    - "Dashboard app starts on localhost:3000 and shows a page"
    - "Booking app starts on a separate port and shows placeholder page"
    - "All workspace packages resolve correctly (shared, odoo-client, ai, tsconfig)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turborepo build orchestration"
      contains: "dependsOn"
    - path: "packages/shared/src/tenants.ts"
      provides: "Tenant registry with subdomain-to-database mapping"
      exports: ["getTenantBySubdomain", "getAllTenants", "Tenant"]
    - path: "packages/shared/src/types/auth.ts"
      provides: "Auth session types"
      exports: ["User", "Session"]
    - path: "packages/ai/package.json"
      provides: "AI package stub for future phases"
      contains: "@clubos/ai"
    - path: "packages/ai/src/index.ts"
      provides: "AI package entry point placeholder"
  key_links:
    - from: "apps/dashboard/package.json"
      to: "packages/shared"
      via: "workspace dependency @clubos/shared"
      pattern: "@clubos/shared"
    - from: "apps/dashboard/package.json"
      to: "packages/odoo-client"
      via: "workspace dependency @clubos/odoo-client"
      pattern: "@clubos/odoo-client"
---

<objective>
Restructure the existing create-next-app into a Turborepo monorepo with two apps (dashboard, booking) and shared packages (shared, ai stub, tsconfig). Includes tenant registry types but NOT auth or middleware wiring.

Purpose: Creates the monorepo skeleton that all subsequent plans build on. Structural only -- no auth, no middleware, no login pages.
Output: Working Turborepo monorepo with `pnpm turbo build` passing, apps/dashboard running, apps/booking stub running, packages/shared with tenant types, packages/ai stub created.
</objective>

<execution_context>
@C:/Users/BankAnantravanich/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BankAnantravanich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-odoo/01-RESEARCH.md
@.planning/phases/01-foundation-odoo/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- executor needs these contracts -->

From packages/odoo-client/src/client.ts:
```typescript
export interface OdooConfig {
  url: string;
  db: string;
}

export class ClubOSClient {
  constructor(config: OdooConfig);
  authenticate(login: string, password: string): Promise<number>;
  searchRead<T>(model: string, domain: OdooDomain, fields: string[], options?: OdooSearchReadOptions): Promise<T[]>;
  create(model: string, values: Record<string, any>): Promise<number>;
  write(model: string, ids: number[], values: Record<string, any>): Promise<boolean>;
  unlink(model: string, ids: number[]): Promise<boolean>;
}
```

From packages/odoo-client/src/errors.ts:
```typescript
export class OdooAuthError extends Error {}
export class OdooRPCError extends Error {}
export class OdooConnectionError extends Error {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure into Turborepo monorepo with shared packages</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    .npmrc,
    apps/dashboard/package.json,
    apps/dashboard/next.config.ts,
    apps/dashboard/tsconfig.json,
    apps/booking/package.json,
    apps/booking/app/layout.tsx,
    apps/booking/app/page.tsx,
    apps/booking/next.config.ts,
    apps/booking/tsconfig.json,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/types/auth.ts,
    packages/shared/src/types/tenant.ts,
    packages/shared/src/types/odoo.ts,
    packages/shared/src/tenants.ts,
    packages/shared/src/index.ts,
    packages/ai/package.json,
    packages/ai/tsconfig.json,
    packages/ai/src/index.ts,
    packages/tsconfig/base.json,
    packages/tsconfig/nextjs.json,
    packages/tsconfig/package.json
  </files>
  <action>
CRITICAL: This restructures the existing create-next-app into a monorepo. The current app/ directory becomes apps/dashboard/app/.

1. **Install pnpm globally and delete npm artifacts:**
   - `npm install -g pnpm turbo`
   - Delete `node_modules/` and `package-lock.json` from project root

2. **Create root monorepo config:**
   - `pnpm-workspace.yaml`: packages: ['apps/*', 'packages/*']
   - `.npmrc`: `shamefully-hoist=true` (safety net per Pitfall 3 in research)
   - Root `package.json`: name "clubos", private true, devDependencies: turbo. Scripts: dev (`turbo dev`), build (`turbo build`), lint (`turbo lint`), test (`turbo test`)
   - `turbo.json`: Per research Pattern -- build depends on ^build, dev is persistent/no-cache, lint depends on ^build, test depends on ^build. Outputs: [".next/**", "!.next/cache/**", "dist/**"]

3. **Create packages/tsconfig/ (shared TypeScript configs):**
   - `packages/tsconfig/package.json`: name "@clubos/tsconfig"
   - `packages/tsconfig/base.json`: strict, ES2022, Node16 module resolution, declaration
   - `packages/tsconfig/nextjs.json`: extends base, adds jsx preserve, Next.js paths

4. **Move existing app to apps/dashboard/:**
   - Move `app/`, `public/`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs` into `apps/dashboard/`
   - Create `apps/dashboard/package.json`: name "@clubos/dashboard", dependencies: next 16.x, react 19.x, @clubos/shared, @clubos/odoo-client, @clubos/ui (placeholder)
   - Update `apps/dashboard/tsconfig.json` to extend @clubos/tsconfig/nextjs.json
   - Update `apps/dashboard/next.config.ts` to add `transpilePackages: ['@clubos/ui', '@clubos/shared', '@clubos/odoo-client']` (per Pitfall 4 in research)

5. **Create apps/booking/ (stub):**
   - `apps/booking/package.json`: name "@clubos/booking"
   - `apps/booking/app/layout.tsx`: minimal layout
   - `apps/booking/app/page.tsx`: placeholder "ClubOS Booking Portal -- Coming Soon"
   - Minimal next.config.ts, tsconfig.json

6. **Create packages/shared/:**
   - `packages/shared/package.json`: name "@clubos/shared", main "dist/index.js", types "dist/index.d.ts"
   - `packages/shared/tsconfig.json`: extends @clubos/tsconfig/base.json
   - `packages/shared/src/types/auth.ts`: User type (id, name, email, tenantId, role: 'staff' | 'member'), Session type
   - `packages/shared/src/types/tenant.ts`: Tenant type (id, db, name, subdomain)
   - `packages/shared/src/types/odoo.ts`: Re-export types from odoo-client that apps need (OdooConfig, OdooDomain)
   - `packages/shared/src/tenants.ts`: Static tenant registry per research (nevelmeade -> clubos_nevelmeade, demo -> clubos_demo). Exports: getTenantBySubdomain, getAllTenants, TENANTS map
   - `packages/shared/src/index.ts`: Re-export all types and tenants

7. **Create packages/ai/ (stub):**
   - `packages/ai/package.json`: name "@clubos/ai", version "0.1.0", main "dist/index.js", types "dist/index.d.ts"
   - `packages/ai/tsconfig.json`: extends @clubos/tsconfig/base.json
   - `packages/ai/src/index.ts`: Export placeholder -- `export const AI_VERSION = '0.1.0';` and a comment: "// AI package stub -- dynamic pricing, churn prediction, member insights (Phase 2+)"

8. **Integrate packages/odoo-client from Plan 01:**
   - Update `packages/odoo-client/package.json` to reference @clubos/tsconfig
   - Ensure it's listed in pnpm-workspace.yaml (already covered by packages/*)

9. **Run `pnpm install` and `pnpm turbo build` to verify everything compiles.**

PITFALL WARNING: After migration from npm to pnpm, if modules aren't found, check that `shamefully-hoist=true` is in .npmrc. Delete all node_modules before pnpm install.
  </action>
  <verify>
    <automated>pnpm install && pnpm turbo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Monorepo structure: apps/dashboard, apps/booking, packages/shared, packages/odoo-client, packages/ai, packages/tsconfig
    - `pnpm turbo build` completes without errors
    - Dashboard app runs on localhost:3000
    - Booking app runs on a separate port
    - packages/ai stub exists with package.json and src/index.ts
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` -- full monorepo build succeeds
2. Start dashboard: `pnpm dev --filter @clubos/dashboard` -- loads on localhost:3000
3. Start booking: `pnpm dev --filter @clubos/booking` -- loads on a different port
4. `ls packages/ai/src/index.ts` -- AI stub exists
5. `pnpm ls -r --depth 0` -- all workspace packages listed
</verification>

<success_criteria>
- Turborepo monorepo builds cleanly with pnpm (apps/dashboard, apps/booking, packages/shared, packages/odoo-client, packages/ai, packages/tsconfig)
- Dashboard app starts and renders
- Booking app starts and shows placeholder
- packages/ai stub created with package.json and entry point
- No auth, no middleware, no login pages in this plan (those come in Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-odoo/01-02-SUMMARY.md`
</output>
